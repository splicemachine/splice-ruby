describe "Welcome", :type => :feature do

  context "As not subscribed user" do
    # before {visit root_path}

    it "should sign up successfully - tests slug that is necessary", js: true do
      # expect(page.body).to have_content("Rails")
    end
  end
end